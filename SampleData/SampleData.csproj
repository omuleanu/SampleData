<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AssemblyName>Omu.SampleData</AssemblyName>
    <RootNamespace>Omu.SampleData</RootNamespace>
    <PackageProjectUrl>https://github.com/omuleanu/SampleData</PackageProjectUrl>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Authors>Ribor</Authors>
    <Company>Ribor</Company>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ValueInjecter" Version="3.1.3" />
  </ItemGroup>

</Project>
